const sum = function (number1: number, number2: number): number {
    return number1 + number2
}

let result: number = sum(1, 2)

console.log(result)

const subtract = (number1: number, number2: number): number => {
    return number1 - number2
}

let resultSubtract: number = subtract(5, 2)

console.log(resultSubtract)